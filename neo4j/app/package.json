{
  "name": "app",
  "version": "1.0.0",
  "description": "1. Create a docker container for Neo4j and run it: ```bash $> docker run -dit --rm --name=my-neo4j -p 7474:7474 -p 7687:7687 --env=NEO4J_AUTH=none neo4j $> docker exec -it my-neo4j cypher-shell ```",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1",
    "neo4j-driver": "^4.4.6",
    "nodemon": "^2.0.19"
  }
}
