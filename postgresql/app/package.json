{
  "name": "app",
  "version": "1.0.0",
  "description": "1. Run the postgresql docker ```bash $> docker run --name my-postgres -e POSTGRES_PASSWORD=123 -p 5432:5432 -d --rm postgres $> docker exec -it -u postgres my-postgres psql  ```",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1",
    "nodemon": "^2.0.19",
    "pg": "^8.7.3"
  }
}
