{
  "name": "app",
  "version": "1.0.0",
  "description": "1. First run the redis docker container ```bash $> docker run -dit --rm --name=my-redis -p 6379:6379 redis $> docker exec -it my-redis redis-cli ```",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1",
    "nodemon": "^2.0.19",
    "redis": "^4.2.0"
  }
}
